// fiveserver.config.js
module.exports = {
    host: '0.0.0.0', // default: '0.0.0.0' (binds to all available network interfaces)
    remoteLogs: 'magenta', // true | false | Color
    useLocalIp: true, // optional: opens browser with your local IP
}